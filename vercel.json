{
  "buildCommand": "npm run build --prefix client",
  "outputDirectory": "client/dist",
  "installCommand": "npm run install:all",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://YOUR-API-DEPLOYMENT-URL/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
